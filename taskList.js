/*
  NOTE: Where did I leave off?


  TODO: Backlog
  - rebuild all products since the schema has changed.

  - re-write User Registration backend logic to also create a MarketHero lead.

  - Add error msg handling to all model methods on all models & Update all GraphQL types to query for the error object in response.

  - Build "Transaction" model & schema.

  - Extract tax rate from Square API.
*/
