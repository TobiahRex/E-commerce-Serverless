import React from 'react';
import { Route } from 'react-router';
import NotFound from '../Pages/404/notFound404';

const authRoutes = () => (<Route component={NotFound} />);
export default authRoutes;
